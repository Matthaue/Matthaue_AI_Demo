# 密钥扫描白名单
#
# 被误报的文件写在这里，每行一个路径（相对仓库根目录，支持 * 通配符）。
# 也可以不改这里，直接在误报的那行代码末尾加 `# nosecret` 注释。
#
# 注意：白名单请只放确认无害的文件，不要为了图省事把真实密钥文件加进来。
#
# 示例：
# backend/config.example.py
# docs/*/sample_credentials.json
