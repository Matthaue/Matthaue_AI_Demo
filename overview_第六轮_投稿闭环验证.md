# 第六轮收尾：投稿功能实测 + ima 入库闭环验证（完成）

## 本轮目标（用户要求）
1. 自己测试投稿功能
2. 验证投稿是否会上传到 ima
3. ima 技巧文档标注来源（官网/用户上传）
4. 用户上传条目保存 user_id、时间、点赞、评论

## 验证结论（全部通过）

### ① 投稿功能前端实测 ✅
用 playwright-core + 系统 Chrome headless 打开工作台 → 切社群投稿栏 → 真实表单提交投稿 → 点赞 → 评论：
- 匿名用户 ID 自动生成并持久化：`u_mt8eu65uftnc`
- 投稿、点赞、评论均正确写入 localStorage，投稿流渲染正确，无 console 错误
- 截图存档：`ima_export/投稿功能实测_社群页.png`

### ② 投稿是否上传到 ima ✅（有，需经"导出→审核→入库"）
纯前端无后端，**投稿不能自动直传 ima**（COS 临时凭证不能进浏览器，否则凭证泄露）。当前闭环：
`本机表单（全元数据）→ 导出投稿包 JSON → gen 脚本转 ima 标准模板 → 人工审核 → ima 入库`
实测投稿已真实入库：知识库 valorantHeroKnow **13 条**（README v3 + 3 英雄 + 7 地图 + 2 条用户投稿），新增条目 `社群投稿_英雄_捷风 Jett_2026-08-25.md` 解析完成（parse 100%），intro 可检索可读。
**自动直连 ima（云函数/后端审核流）列入 V1 迭代。**

### ③ 来源标注 ✅
每条入库内容带 `source` 标注：`source: official`（官网/官方整理）/ `source: user`（用户上传）。工作台投稿流、展播栏、速记卡均显示来源徽标（"用户上传"/"官网来源"）。

### ④ 用户上传元数据 ✅
用户上传条目完整保存并随 ima 落库：`user_id` / `ts`（投稿时间）/ `likes`（点赞数）/ `comments`（评论列表）/ 署名。实测条目库内可读：用户ID u_mt8eu65uftnc、2026-08-25、点赞 1、评论 1。

## 同步更新的文件
- `补位急救站_workbench.html`：badge 与管线说明更新为 13 条 / 闭环已实测
- `WeGame_Agent_产品文档.md/.html`：内容库段落与 M0 行 12→13，闭环已实测
- `ima_export/README_内容库说明.md`：v3（13 条 + 2 条用户投稿），已 REPLACE 覆盖入库
- `ima_export/gen_submission_entries.py`：投稿包 → ima 条目模板转换（含来源标注表 + 元数据）

## 待办（长期）
- 3 个英雄数值/译名对照官方 Wiki 校对
- 扩 Top 10 英雄
- 自录技能动图回填 gif 字段
- 录 2 分钟 Demo 视频
