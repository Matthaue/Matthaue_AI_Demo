# 第八轮 · 后端 CORS 专项检查与健壮性加固

## 一、本轮做了什么

起因是用户一句检查：「后端有没有解决跨域问题」。

代码审查发现 CORS 头**确实配了**，但只覆盖了正常路径。于是没有止步于"看代码说配了"，而是做了一轮实证检查——结果在异常路径和并发路径上各挖出一个真问题：

1. **服务端异常时连接直接断开**，连 HTTP 500 都不返回 → 前端只能拿到 `Failed to fetch`，把真实错误伪装成网络故障
2. **静态资源 404 不带 CORS 头** → 跨域下浏览器先报 CORS 错误，掩盖真正的 404
3. **并发投稿 ID 会碰撞**（压测才发现）→ 30 并发 2 次 `UNIQUE constraint failed`

三个问题全部修复，并用**真实浏览器**（而非 curl）逐项验证通过。

## 二、关键文件变更

| 类型 | 文件 | 说明 |
|---|---|---|
| 改动 | `backend/server.py` | 全局异常兜底、重写 `send_error`、`_s()` 输入校验、`Max-Age`、favicon 补头、ID 生成改 uuid 段 + 重试 |
| 改动 | `backend/README.md` | 新增「CORS 跨域处理」「错误处理」两节（含生效场景表、验证脚本表） |
| 新增 | `ima_export/cors_test.html` | CORS 测试页（5 个场景） |
| 新增 | `ima_export/verify_cors.js` | 真实浏览器跨域验证（起 3000 端口敌对源） |
| 新增 | `ima_export/verify_cors_file.js` | `file://` 场景验证（origin=null） |
| 新增 | `ima_export/stress_concurrent.py` | 30 并发写压测 + CORS 头校验 |
| 新增 | `ima_export/pack_deliverable.py` | 打包流程脚本化（此前每轮内联重写一遍） |

## 三、修复的三个问题

| 问题 | 修复前 | 修复后 |
|---|---|---|
| **服务端异常** | 未捕获异常 → 连接断开、无任何响应。前端 `fetch` 抛 `TypeError: Failed to fetch`，用户误判为网络故障 | 重写 `handle_one_request` 兜底：转 JSON 500 + 完整 CORS 头，控制台打印 traceback |
| **静态 404** | 走 `send_error()`，无 CORS 头 → 跨域下浏览器报 CORS 错误，掩盖真实 404 | 重写 `send_error` 自行构造响应（含 CORS 头） |
| **并发 ID 碰撞** | `毫秒时间戳 + 1 位随机字母`，同毫秒仅 26 种取值 → 30 并发实测 2 次 UNIQUE 冲突 | `毫秒时间戳(hex) + uuid4 前 8 位` + UNIQUE 冲突重试 5 次 → 30/30 |

另外补齐的两项：

- **`Access-Control-Max-Age: 86400`**：缓存预检结果 24h，省掉每个 POST（非简单请求）前的一次 OPTIONS 往返
- **`_s()` 输入校验**：所有用户输入统一转字符串并限长，非法类型降级为默认值而非抛异常

## 四、实测结果

**关键认知：curl 不执行 CORS 策略**，缺 CORS 头的响应它照收不误。只有真实浏览器会真正拦截，所以必须用浏览器验证。

| 验证 | 场景 | 结果 |
|---|---|---|
| `verify_cors.js` | 3000 端口敌对源 → 8000 API（浏览器判定 `localhost ≠ 127.0.0.1`，构成真实跨域） | 5/5 |
| `verify_cors_file.js` | `file://` 双击 HTML（origin = null） | 5/5 |
| `stress_concurrent.py` | 30 并发写 + 每个响应校验 CORS 头 | 30/30 |
| `verify_backend.js` | 前端端到端（回归，确认没改坏） | 点赞 1→2、DB likes 5→6 真实写回，页面错误 0 |

覆盖的 5 个 CORS 场景：简单 GET、触发预检的 POST、异常输入 400、静态 404、DELETE 预检。

## 五、踩过的坑

1. **`send_error` 不能转发给 super**：基类内部会自己走完 `send_response + end_headers`，转发后没有任何插入自定义响应头的机会。只能自己构造完整响应体（含 HTML 错误页 + CORS 头）。
2. **`send_response` 需要埋点**：全局兜底要判断"响应是否已开始"，否则异常发生在 headers 之后时会二次写入导致协议错误。用重写 `send_response` 设置 `_resp_started` 标记解决。
3. **测试之间有数据依赖**：`verify_cors.js` 的 DELETE 用例会清空全部投稿，导致随后跑 `verify_backend.js` 时 feed 里没有 `.feed-item`、定位超时 30s。真不是代码 bug——必须先 `POST /api/reset` 再跑端到端。
4. **Bash heredoc 写含 `${r.pass}` 的 JS 会被 Git Bash 当变量替换**，报 `Bad substitution`。改用文件写入工具。

## 六、面试可直接讲的话术

**Q：你的后端怎么处理跨域？**

> "**结论：配了 `Access-Control-Allow-Origin: *`，但要说清楚它什么时候才生效。** 我的后端自带静态托管，页面从 `http://127.0.0.1:8000/` 打开时跟 API 是同源的，**CORS 根本不触发**。它真正起作用的是两个场景：一是用户直接双击 HTML（`file://`，origin 是 null，`*` 可以放行）；二是以后前端独立部署到别的域名。**所以我配 CORS 不是为了现在，是为了让双击也能连后端、以及未来前后端分离时后端不用改。** 另外我加了 `Max-Age: 86400` 缓存预检，省掉每个 POST 前的一次 OPTIONS 往返。"

**Q：后端出错了，用户会看到什么？**

> "**这恰恰是我花最多力气的地方。** 最初代码里异常处理有个很隐蔽的坑：一旦抛出未捕获异常，连接会直接断开、**连 500 都不返回**，前端只能拿到 `Failed to fetch`。用户会以为是网络断了或者服务挂了，实际可能只是一个输入类型不对。**这种错误最误导排查**——它把服务端 bug 伪装成网络故障。所以我加了全局兜底：任何未捕获异常都转成带 CORS 头的 JSON 500，带上真实错误类型，同时控制台打完整栈。404 也一样，补了 CORS 头，否则跨域下浏览器会先报 CORS 错误，把真正的 404 盖住。"

**Q：并发情况下你怎么保证数据不出问题？**

> "**我做了压测，而且压测确实抓到了 bug。** 写操作我用 `threading.Lock` 串行化——SQLite 并发写会 `database is locked`，加锁后这条路径是安全的。但压测 30 并发时**仍有 2 次失败**，报 `UNIQUE constraint failed`。查下来是投稿 ID 的生成算法有问题：我原来写的是『毫秒时间戳 + 1 位随机字母』，同一毫秒内只有 26 种取值，撞车概率很高。**这个 bug 靠读代码是发现不了的，只有压测能暴露。** 改成『毫秒时间戳加 uuid 随机段』，再加一层冲突重试，现在 30 并发 30/30 全过。"

> 💡 这段的价值在于：它证明你**真的跑过压测**，而不是嘴上说"我考虑了并发"。面试官追问"你怎么验证的"时能答出具体数字。

## 七、一个值得记住的认知

**CORS 配置与部署方式强相关**：服务绑 `127.0.0.1` + 自带静态托管 = 同源，CORS 不参与。所以看到"CORS 配了却没生效"别急着改配置，先确认是不是根本没触发跨域。

同理，若将来要真正演示"多人共享同一份数据"（局域网内其他设备访问），需要把绑定改成 `0.0.0.0`，**那时 CORS 才有实际意义**。

## 八、可复用经验

1. **验证 CORS 必须用真实浏览器**：curl 不执行 CORS 策略，会给出假阳性结论。做法是起一个不同端口/不同 host 的"敌对源"页面，让浏览器去判定。
2. **异常路径要和正常路径一样测**：只测 happy path 会漏掉最误导人的错误。专门构造非法输入（类型错误、超长、空值）看服务端反应。
3. **并发压测能发现读代码发现不了的 bug**：尤其是 ID 生成、计数器、唯一约束这类"看起来没问题"的地方。
4. **错误消息的可观测性优先于优雅**：宁可返回丑陋但准确的 JSON 500，也不要让连接静默断开。
